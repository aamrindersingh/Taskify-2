Create a file named ".env" in the backend folder with exactly this content:

# Database Configuration
MONGODB_URI=mongodb+srv://1234captain12_db_user:9TuL8URMMmCwPVut@cluster0.sbhtvuo.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (CRITICAL - this must be set!)
JWT_SECRET=taskify_super_secret_jwt_key_for_development_environment_2024_make_this_very_long_and_secure
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:3000

IMPORTANT NOTES:
1. Replace the MONGODB_URI with your actual MongoDB Atlas connection string
2. The JWT_SECRET can be the one shown above for development
3. The file must be named exactly ".env" (with the dot)
4. Place it in the backend folder (same level as package.json)
5. After creating this file, restart the backend server

The "invalid token" error means the JWT_SECRET is missing or incorrect!
